# CoreClaw Environment Configuration
# Copy this file to .env and fill in your values

# Anthropic API key — required for agents inside containers
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# SQLite database path (default: data/coreclaw.db)
# DB_PATH=data/coreclaw.db

# Docker image for agent containers (default: coreclaw-agent:latest)
# AGENT_IMAGE=coreclaw-agent:latest
